#!/usr/bin/env ruby

require 'crf'
require 'optparse'

options = { interactive: false, progress: false }

parser = OptionParser.new do |opts|
  opts.banner = 'Usage: crf PATH [-i] [-p]'

  opts.on('-i', '--interactive', 'Command line interactive file removal') do
    options[:interactive] = true
  end

  opts.on('-p', '--progress', 'Show a progress bar while executing') do
    options[:progress] = true
  end

  opts.on('-h', '--help', 'Displays help') do
    puts opts
    exit
  end
end

parser.parse!

Crf::Checker.new(ARGV[0], options).check_repeated_files
